import React from 'react'

export default function ListItem({item}) {
  return (
    <div>
      <img src='' alt='' />
      <p>${price}</p>
      <p>${stock}</p>
      <p>${description}</p>
    </div>
  )
}
